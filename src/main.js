import Vue from 'vue'
import App from './App.vue'

new Vue({
  el: '#app',
  render: h => h(App)
})
// import History from './history'
// const cache = new History()
// cache.insert(11).insert(1234)

// window.cache = cache